[pytest]
testpaths = tests
asyncio_mode = auto
python_files = test_*.py
python_functions = test_*
markers =
    integration: tests that require real Redis/Mongo via testcontainers (Docker)
filterwarnings =
    ignore::DeprecationWarning
